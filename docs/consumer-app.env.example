# Copy this file into a consuming Expo app as `.env.local`.
# Every EXPO_PUBLIC_* value is visible in the compiled mobile bundle.
EXPO_PUBLIC_MOBILE_SYNC_URL=https://sync.example.com
# The current baseline supports native Google on Android only.
EXPO_PUBLIC_MOBILE_SYNC_PROVIDERS=
